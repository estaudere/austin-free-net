import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.austinfree.net',
  appName: 'Austin Free Net',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
